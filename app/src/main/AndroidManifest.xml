<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PeakPal 앱 권한 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 카메라 하드웨어 기능을 선택사항으로 설정 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".TrailMateApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrailMate"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrailMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 카카오맵 인증 테스트 액티비티 -->
        <activity
            android:name=".ui.test.KakaoMapAuthTestActivity"
            android:exported="false"
            android:label="카카오맵 인증 테스트"
            android:theme="@style/Theme.TrailMate" />
            
        <!-- 카카오맵 로딩 테스트 액티비티 -->
        <activity
            android:name=".ui.test.KakaoMapLoadTestActivity"
            android:exported="false"
            android:label="지도 로딩 테스트"
            android:theme="@style/Theme.TrailMate" />
            
        <!-- 초기 지도 다운로드 액티비티 -->
        <activity
            android:name=".ui.initial.InitialMapDownloadActivity"
            android:exported="false"
            android:label="지도 데이터 준비"
            android:theme="@style/Theme.TrailMate" />
            
        <!-- 카카오맵 종합 테스트 액티비티 -->
        <activity
            android:name=".ui.test.KakaoMapCompleteTestActivity"
            android:exported="false"
            android:label="카카오맵 종합 테스트"
            android:theme="@style/Theme.TrailMate" />
    </application>

</manifest>